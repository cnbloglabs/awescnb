// 打字效果
const typed = () => {
    const options = {
        strings: ['<i>First</i> sentence.', '&amp; a second sentence.'],
        typeSpeed: 40,
    }
    var typed = new Typed('.element', options)
}
